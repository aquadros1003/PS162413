#include <stdio.h>
#include <stdlib.h>
int *kolokwium_5(int n, int tab)
{
	int* wsk;
	for (int i = 0; i < n; i++)
	{
		if (tab[i] % 2 == 0)
			*wsk = tab[i];
		return *wsk;
		break;
	}
	*wsk = tab[0]
		return *wsk;
}
